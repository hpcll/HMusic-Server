# 更新日志

## Unreleased

- 后续变更将在此记录。

## 0.3.0 - 2026-09-15

- **新增 Spotify 接入（实验性）**：在服务器电脑上登录 Spotify 网页版后，服务端自动读取会话，
  经 Web Player 的 Pathfinder 私有接口读取个人常听曲目和歌单，再用 HMusic 已配置的音源搜索
  匹配播放。该通道依赖 Spotify 网页 token、client-token 接口和社区维护的 TOTP 密钥，服务端
  需要能访问 Spotify 和 GitHub；Pathfinder 属网页端私有接口，Spotify 更新网页后可能需要重新
  适配，不可用时回退官方 API。会话 Cookie 加密保存，旧版明文会话首次读取时自动迁移；删除
  HMusic 账号会一并清除 Spotify 缓存和未完成任务。接口清单见 README。
- **榜单页重做为卡片墙**：新增 Spotify 个人常听榜（近四周 / 近半年 / 长期各 50 首）和公开榜
  （全球 Top 50、Viral 50·全球、Top 50·中国香港），与网易云、QQ 音乐、Apple Music、HMusic
  榜单并列展示。支持按来源筛选、卡片内前 3 首预览、骨架屏加载、失败重试和一键播放。未绑定
  Spotify 时榜单目录自动隐藏 Spotify 条目，只显示原有榜单。
- **Spotify 歌单并入「歌单」页**，侧栏不再单列 Spotify 入口，Spotify 页只负责账号绑定与状态；
  可从 **设置 → 账号与设备 → Spotify** 或歌单页的「Spotify 设置」按钮进入。
- Spotify 播放匹配会校验歌名和歌手：首曲匹配成功后立即入队，其余曲目在后台继续匹配，可在
  队列页查看结果；发起新的 Spotify 播放请求、清空或替换队列、解除绑定都会停止旧任务。
- 榜单、搜索和歌单封面改用 500-600px 图源，高分屏下不再模糊。
- 榜单与 Spotify 内容按天缓存，上游限流或失败时回退到上一份可用缓存，不把限流错误显示成空榜。
- README 补充 Spotify 接口说明、分页约定和登录/解绑行为。

## 0.2.3 - 2026-08-30

- 修复 **App 里点歌就播放失败，同一台服务端的网页端却一切正常**：音频代理把客户端自报的
  `User-Agent` 原样转给上游音源，而 App 播放器送的是 `ExoPlayer/…`（Android）或 `AVPlayer`
  （iOS/macOS）这类 UA——第三方音源的 CDN 按 UA 放行，这些 UA 会被直接 403 或重定向到错误页；
  浏览器 `<audio>` 送的是浏览器 UA，所以只有网页端能播。现在代理统一用固定的浏览器 UA 取上游，
  不再透传客户端 UA。`Range` 仍原样带过去，拖进度和断点续拉不受影响。

## 0.2.2 - 2026-08-28

- 管理页新增「设置 → 系统 → 关于与更新」：显示当前版本与部署方式、检查 GitHub 新版、
  一键升级（带确认与进度轮询）、查看升级日志。此前这三个接口只有 App 在用，只看网页的用户
  无从知道有新版。
- 升级文档补充一键升级的排查路径，包含守护不在线时如何用 `HMUSIC_UPDATER_URL` 指向容器地址。

## 0.2.1 - 2026-08-28

- 修复 Docker 升级守护 `hmusic-updater` 无限重启：compose 曾给 watchtower 传了它不存在的
  `--http-api-port`，参数解析失败即退出，被 `restart: unless-stopped` 反复拉起。守护改用桥接网络
  加 `127.0.0.1:8666:8080` 端口映射对齐服务端调用地址，镜像固定到 `containrrr/watchtower:1.7.1`。
- 安装器在容器健康检查通过后确认升级守护真的在运行，未运行时打印状态和排查命令，不再静默通过。
- 管理页登录失败不再一律显示「登录已失效，请重新登录」：密码错误照实回显后端文案，代理删改
  `Authorization` 头、401 来自代理而非服务端等情况各自给出对应提示。
- 401 响应保留 `@fastify/jwt` 的原始错误 code，`GET /api/v1/auth/status` 在校验失败时返回
  `authError`；反向代理到公网时据此区分「凭据没送到」和「凭据被拒」。
- 增加反向代理与公网访问部署说明，含 Nginx 最小配置、登录报错对照表和自查命令。
- 升级提示：`hmusic-updater` 的修复在宿主机的 `docker-compose.yml` 里，只拉新镜像不生效。
  Docker 部署请在安装目录执行 `bash install.sh --update`；守护崩溃循环期间 App 内的一键升级本就不可用。

## 0.2.0 - 2026-08-26

- 增加公开部署指南、贡献指南、安全报告入口和发布验收说明。
- 增加 Docker 与原生安装的统一升级、停止和健康检查流程。
- 安装器支持通过可信镜像代理下载 GitHub Release，升级时保留配置、账号和数据。
- 改进小爱音箱机型播放与 TTS 兼容，并公开注明对 SongLoft MIoT 插件与 xiaomusic 的参考及许可证。
- 旧版 HMusic App 继续可用，新版页面和接口作为推荐入口。
- 拒绝使用空值或默认值启动服务 JWT 密钥，并统一服务端版本信息。

版本发布时记录 API 变化、数据库迁移、部署方式和已知限制。Git tag 使用 `vX.Y.Z`，并与
`package.json` 版本保持一致。
